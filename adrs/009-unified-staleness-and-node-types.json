{
  "title": "ADR-009: Unified Staleness and Node Types",
  "tags": [
    "adr",
    "staleness",
    "ontology",
    "schema",
    "v2"
  ],
  "sections": [
    {
      "id": "status",
      "heading": "Status",
      "content": "**Status:** Accepted\n\n**Date:** 2026-03-15\n\n**Decider:** Dante\n\n**Supersedes:** ADR-007 (Node Type Unification), ADR-008 (Persisted Staleness)"
    },
    {
      "id": "context",
      "heading": "Context & Problem Statement",
      "content": "Five compounding problems exist in the current data model:\n\n**1. Ontology violations.** `document` nodes use `composes` edges to link doc files to their sections. The ontology only permits `composes` between `composite_process` and `atomic_process`. Every doc file produces one violation per section — currently 426 warnings on every `cortex build`.\n\n**2. Missing own-content staleness for doc sections.** Doc sections have `node_type=document` and are never treated as first-class nodes with hashable content. Their own prose changing is undetected. The `_SECTION_CODE_HASH = \"doc_section\"` constant means sections have a dummy `code_hash` and store prose in `desc_hash` — an inversion of the intended semantics where `code_hash` should hold the primary content hash.\n\n**3. Staleness vocabulary is fragmented and computed on-demand from four divergent paths.**\n- `CODE_STALE` (function body changed) and the old `CONTENT_STALE` (doc's linked code changed) use different names for structurally identical signals.\n- `COVERAGE_STALE` (test's validated code changed) and old `CONTENT_STALE` are the same signal over different edge types.\n- `_compute_staleness_from_history` (history-table, no file I/O) and `_compute_staleness_rich` (re-parses files, authoritative) routinely disagree — e.g. 67 vs 171 CODE_STALE on the same project.\n- `_compute_staleness_for_nodes` in the MCP server is a **third** independent implementation with its own divergences: no epsilon on mtime comparison, no composite inheritance, no document overlay, promotes `UNVERIFIED → VERIFIED` (others don't), and assigns `UNVERIFIED` for missing `code_hash` (others assign `CLEAN`).\n- `get_stale_tests()` queries `WHERE node_type = 'constraint'` but the scanner creates tests as `atomic_process` — so `COVERAGE_STALE` never fires in production.\n- All three workflows share the same \"both changed → CLEAN\" bug: when both `code_hash` and `desc_hash` change, the else branch emits `CLEAN` instead of stale.\n\n**4. `node_type` has five overlapping values.** `document`, `constraint`, `atomic_process`, `composite_process`, `entity`. Combined with the sparse `subtype` field this creates ambiguity about what a node actually is. The module scanner creates test functions as `atomic_process` with a `\"test\"` tag but no `subtype=\"test\"`, compounding the `get_stale_tests` bug.\n\n**5. Verification is incomplete.** The `node_verification` table only stores `code_hash_at`, not `desc_hash_at`. A `desc_hash` change after verification goes undetected — the node stays `VERIFIED`. Additionally, `verify_node()` in the viz server rejects all `node_type=\"document\"` nodes, making doc sections unverifiable even though they have reviewable content.\n\n**6. Composite inheritance silently drops unknown statuses.** The `_SEVERITY` map in `staleness.py` only contains `CLEAN`, `VERIFIED`, `DESC_STALE`, `CODE_STALE`, and `STRUCTURAL_DRIFT`. Statuses like `CONTENT_STALE` and `COVERAGE_STALE` silently map to severity 0 (`CLEAN`) during composite inheritance, hiding problems from parent nodes."
    },
    {
      "id": "decision",
      "heading": "The Decision",
      "content": "This ADR combines ADR-007 (node type unification) and ADR-008 (persisted staleness) into a single change to avoid throwaway work.\n\n### Part 1 — Unify node_type to three values\n\n`node_type` is reduced to three structural values that describe containment shape only:\n\n- `composite_process` — a container that composes children\n- `atomic_process` — a leaf node with hashable content\n- `entity` — a data artifact, never stale\n\n`document` and `constraint` are retired as node_type values.\n\n### Part 2 — subtype becomes the full differentiator\n\n`subtype` is now required for `atomic_process` and `composite_process` nodes:\n\n| node_type | subtype | what it is |\n|---|---|---|\n| `composite_process` | `module` | Python module |\n| `composite_process` | `docjson` | DocJSON doc file |\n| `composite_process` | `external_package` | External dependency |\n| `atomic_process` | `function` | Python function or method |\n| `atomic_process` | `docjson` | DocJSON doc section |\n| `atomic_process` | `test` | Test function |\n| `entity` | — | Data artifact |\n\nTest functions are identified by file location (test file path patterns) and function name (`test_` prefix). The module scanner must set `subtype=\"test\"` on these nodes (currently it only adds a `\"test\"` tag). No annotation required.\n\n### Part 3 — Doc nodes become first-class process nodes\n\nDocJSON doc files become `composite_process` + `subtype=docjson`. Sections become `atomic_process` + `subtype=docjson`. The `composes` edge between them is now ontologically valid — 426 warnings eliminated.\n\nSections get `code_hash` (prose body hash) and `desc_hash` (heading hash), identical to function nodes. Own-content staleness detection is now active.\n\n**Hash semantic swap:** The current `_SECTION_CODE_HASH = \"doc_section\"` constant is removed. Previously, sections stored a dummy constant in `code_hash` and the prose hash in `desc_hash`. This is inverted: `code_hash` now holds the prose body hash (primary content) and `desc_hash` holds the heading hash (descriptor). This aligns with function nodes where `code_hash` = function body and `desc_hash` = docstring.\n\nDoc sections become verifiable — the `verify_node()` rejection of `node_type=\"document\"` is removed since doc sections are now `atomic_process`.\n\n### Part 4 — Staleness vocabulary: primary and secondary\n\n**Primary staleness** — own content changed, hash comparison, all `atomic_process` nodes:\n\n| Signal | Meaning | Detected by |\n|---|---|---|\n| `CONTENT_STALE` | Body/main content changed | `code_hash` changed, `desc_hash` unchanged |\n| `DESC_STALE` | Descriptor/heading changed | `desc_hash` changed, `code_hash` unchanged |\n\nWhen both `code_hash` and `desc_hash` change, emit `CONTENT_STALE` (higher severity). The user should review the entire node (code and description) before verifying. This fixes the \"both changed → CLEAN\" bug present in all three current staleness workflows.\n\n**Secondary staleness** — a dependency changed, graph edges:\n\n| Signal | Meaning | Edge |\n|---|---|---|\n| `LINKED_STALE` | A node this references changed | `documents` or `validates` |\n| `STRUCTURAL_DRIFT` | File or node no longer exists | existence check |\n\n`CONTENT_STALE` replaces `CODE_STALE`. `LINKED_STALE` replaces both the old `CONTENT_STALE` and `COVERAGE_STALE`.\n\n**Severity order:** `STRUCTURAL_DRIFT > CONTENT_STALE > LINKED_STALE > DESC_STALE > CLEAN`\n\n**Promotion:** `CONTENT_STALE` and `DESC_STALE` are promotable to VERIFIED. `LINKED_STALE` and `STRUCTURAL_DRIFT` are not.\n\n### Part 5 — Persist staleness to the database\n\nA `staleness` column is added to the `nodes` table:\n\n```sql\nALTER TABLE nodes ADD COLUMN staleness TEXT NOT NULL DEFAULT 'CLEAN';\n```\n\n**Single writer:** `_compute_staleness_rich` is extracted to a shared module and is the only function that computes staleness. It re-reads files, computes hashes, and writes the `staleness` column.\n\n**Write points:**\n1. `cortex build` — after indexing, compute and persist staleness for all nodes.\n2. `cortex check` (CLI/MCP) — recompute, write back, return results.\n3. Viz `/api/check` — recompute, write back, return results.\n\n**Read points:** Viz `/api/all`, MCP `cortex_check` (default mode), MCP `cortex_render` — read `staleness` column directly. No computation.\n\n**Deleted:**\n- `_compute_staleness_from_history` (viz server) — history-table path removed entirely.\n- `_compute_staleness_for_nodes` (MCP server) — third divergent implementation removed. MCP write points call the shared extracted staleness logic; MCP read points read the `staleness` column directly.\n- `_refreshStaleness` fire-and-forget in viz JS (`app.js`) — removed. `/api/all` returns the persisted staleness column; no background re-check needed.\n\n### Part 6 — Verification snapshots both hashes\n\nThe `node_verification` table gains a `desc_hash_at` column:\n\n```sql\nALTER TABLE node_verification ADD COLUMN desc_hash_at TEXT;\n```\n\nWhen a user verifies a node, both `code_hash_at` and `desc_hash_at` are stored — a single action that snapshots the full state. Verification means \"I reviewed this entire node (code and description) and it is accurate.\"\n\nOn subsequent staleness checks, promotion to VERIFIED requires **both** hashes to still match the verification snapshot:\n\n| code_hash matches? | desc_hash matches? | Result |\n|---|---|---|\n| yes | yes | VERIFIED |\n| no | yes | CONTENT_STALE |\n| yes | no | DESC_STALE |\n| no | no | CONTENT_STALE |\n\nThis ensures that any future drift in either hash is detected after verification. Previously, only `code_hash` was tracked, so a `desc_hash` change after verification went undetected.\n\n### Part 7 — Ontology edge rule updates\n\nThe `documents` edge type stays (it's a valid semantic relationship). Its `from:` rule changes from `[document]` to `[atomic_process, composite_process]` since doc sections are now `atomic_process`. The `to:` rule drops `document` and `constraint`.\n\nThe `constrains` edge `from:` drops `constraint`, becomes `[atomic_process]`. The `validates` edge `from:` already includes `atomic_process`; `constraint` is dropped.\n\nThe `supersedes` edge `from:` and `to:` drop `constraint`, become `[atomic_process, composite_process]`.\n\n`composes` rules are already correct — no change needed.\n\nThe `document` and `constraint` node types are removed from `cortex_ontology.yaml` entirely. The `NodeType` enum in `ontology.py` drops both values.\n\n**Linked nodes:**\n- `cortex::cortex.cli::cmd_check` — cmd_check(project_root, show_all, fail_on, output_format) — Report per-node staleness / confidence status.  @ cortex/cli.py#L260-L414\n- `cortex::cortex.index.db::get_stale_doc_sections` — get_stale_doc_sections(db_path) — Return doc sections whose linked code node changed after the section was last updated.  @ cortex/index/db.py#L1110-L1140\n- `cortex::cortex.index.db::get_stale_tests` — get_stale_tests(db_path) — Return test nodes whose validated code node changed after the test was last updated.  @ cortex/index/db.py#L1149-L1182\n- `cortex::cortex.index.db::get_undocumented_nodes` — get_undocumented_nodes(db_path) — Return nodes that have no inbound 'documents' edge.  @ cortex/index/db.py\n- `cortex::cortex.index.staleness::apply_composite_inheritance` — apply_composite_inheritance(statuses, db_path) — Assign each composite_process node the worst-severity child status.  @ cortex/index/staleness.py#L33-L117\n- `cortex::cortex.ontology::validate_edge` — validate_edge(edge_type, from_node_type, to_node_type) — Return an error message string if the edge is invalid, else None.  @ cortex/ontology.py#L71-L82\n- `cortex::cortex.scanners.doc_scanner` — Cortex doc scanner — Markdown files → CortexNode + CortexEdge objects.\n- `cortex::cortex.scanners.json_doc_scanner` — Cortex JSON doc scanner — docs/*.\n- `cortex::cortex.mcp_server::_compute_staleness_for_nodes` — _compute_staleness_for_nodes(db_path, root, nodes) — Third divergent staleness implementation (deleted by this ADR).  @ cortex/mcp_server.py#L70\n- `cortex::cortex.viz.server::_compute_staleness_from_history` — _compute_staleness_from_history(nodes) — History-table staleness check with file-existence fast-pass (deleted by this ADR).  @ cortex/viz/server.py#L183-L295\n- `cortex::cortex.viz.server::_compute_staleness_rich` — _compute_staleness_rich(nodes) — Full hash-based staleness check (becomes the single writer, extracted to shared module).  @ cortex/viz/server.py#L303-L445",
      "links": [
        {
          "node_id": "cortex::cortex.index.staleness::apply_composite_inheritance"
        },
        {
          "node_id": "cortex::cortex.viz.server::_compute_staleness_from_history"
        },
        {
          "node_id": "cortex::cortex.viz.server::_compute_staleness_rich"
        },
        {
          "node_id": "cortex::cortex.scanners.json_doc_scanner"
        },
        {
          "node_id": "cortex::cortex.scanners.doc_scanner"
        },
        {
          "node_id": "cortex::cortex.ontology::validate_edge"
        },
        {
          "node_id": "cortex::cortex.cli::cmd_check"
        },
        {
          "node_id": "cortex::cortex.index.db::get_stale_tests"
        },
        {
          "node_id": "cortex::cortex.index.db::get_stale_doc_sections"
        }
      ]
    },
    {
      "id": "implementation-order",
      "heading": "Implementation Order",
      "content": "**Migration strategy:** Re-init (`cortex init`) rather than `ALTER TABLE` migration. Simpler, avoids migration complexity. History and verifications are reset. All existing DBs must be re-initialized.\n\n**Layer 1 — Schema & ontology (foundation)**\n- `cortex_ontology.yaml`: remove `document`/`constraint` node types, update edge rules for `documents`, `constrains`, `validates`, `supersedes`\n- `cortex/ontology.py`: remove `document` and `constraint` from `NodeType` enum\n- DB schema in `db.py`: add `staleness TEXT NOT NULL DEFAULT 'CLEAN'` column to `nodes` table, add `desc_hash_at TEXT` column to `node_verification` table\n\n**Layer 2 — Scanners (node creation)**\n- `json_doc_scanner.py`: doc files → `composite_process`/`subtype=docjson`, sections → `atomic_process`/`subtype=docjson`. Remove `_SECTION_CODE_HASH` constant. Sections get `code_hash` = prose body hash, `desc_hash` = heading hash (swap from current inverted semantics).\n- `doc_scanner.py`: same node_type/subtype changes for markdown docs\n- `module_scanner.py`: test functions get `subtype=test` (currently only tagged, not subtyped). The `_collect_functions` helper must set `subtype=\"test\"` when `\"test\" in func_tags`.\n\n**Layer 3 — Staleness engine (single writer)**\n- Extract `_compute_staleness_rich` to a shared module (e.g. `cortex/index/staleness.py` or new file) so CLI, MCP, and Viz all call the same function\n- `staleness.py` severity map: rename `CODE_STALE` → `CONTENT_STALE`, add `LINKED_STALE` to `_SEVERITY` and `_SEVERITY_TO_STATUS`\n- `_compute_staleness_rich`: rename vocabulary, drop `document`/`constraint` branches, use `subtype` checks. When both hashes change, emit `CONTENT_STALE`. Handle doc sections via the same hash-comparison path as functions (no separate overlay needed — they're `atomic_process` now).\n- Delete `_compute_staleness_from_history` (viz server)\n- Delete `_compute_staleness_for_nodes` (MCP server)\n\n**Layer 4 — Persistence plumbing**\n- `cortex build`: after indexing, run the shared staleness check, write `staleness` column for all nodes\n- `cortex check` (CLI) / MCP `cortex_check` / Viz `/api/check`: run shared staleness check, write back to DB, return results\n- Viz `/api/all`: read `staleness` column directly (no computation)\n- MCP `cortex_render` (default mode): read `staleness` column directly\n- DB queries: fix `get_stale_tests` to use `subtype='test'` instead of `node_type='constraint'`; fix `get_stale_doc_sections` to drop `node_type != 'document'` filter; fix `get_undocumented_nodes` to exclude `subtype='docjson'` instead of `node_type != 'document'`\n\n**Layer 5 — Verification**\n- Verify endpoint: store both `code_hash_at` and `desc_hash_at`\n- Promotion logic: require both hashes to match for VERIFIED, otherwise re-evaluate which staleness applies\n- Remove `verify_node()` rejection of document nodes — doc sections (`atomic_process`/`subtype=docjson`) are now verifiable\n\n**Layer 6 — CLI/MCP/Viz output**\n- `cmd_check`, MCP `cortex_check`: new vocabulary in output (`CONTENT_STALE`, `LINKED_STALE`)\n- MCP `_compute_staleness_for_nodes`: replaced by read from `staleness` column (for default/read mode) or call to shared staleness check (for recompute mode)\n- Viz JS `app.js`: remove `_refreshStaleness` fire-and-forget; `/api/all` now returns persisted staleness directly\n- Viz JS `graph.js`: update Cytoscape CSS selectors for new staleness values (`CONTENT_STALE`, `LINKED_STALE`; remove `CODE_STALE`, `COVERAGE_STALE`)\n- Viz JS `app.js`: update hardcoded staleness filter dropdown values\n- Viz JS `list.js`, `detail.js`: update badge classes\n- Viz: color map by subtype instead of node_type for node coloring\n\n**Layer 7 — Tests**\n- Update `test_staleness_helpers.py` fixtures and assertions (change `node_type=\"constraint\"` to `node_type=\"atomic_process\"`, `subtype=\"test\"`)\n- Update `test_builder_rename_only_for_code_nodes`: doc rename detection now fires (assertion flips from `== 0` to `> 0`), since doc nodes are now `composite_process` and participate in hash-similarity rename detection\n- Add tests for `LINKED_STALE`, persisted staleness column, and dual-hash verification\n- Add test for \"both changed → CONTENT_STALE\" (was previously falling through to CLEAN)"
    },
    {
      "id": "consequences",
      "heading": "Consequences",
      "content": "**Positive:**\n- 426 ontology violations eliminated.\n- Doc sections gain own-content staleness detection.\n- Staleness vocabulary is uniform: `CONTENT_STALE` means body changed everywhere, `LINKED_STALE` unifies two formerly separate signals.\n- Single source of truth for staleness — viz, MCP, CLI all read the same DB column. Three divergent implementations collapsed to one shared writer.\n- `/api/all` is instant (pure DB read, no file I/O).\n- No divergence between history-path, rich-path, and MCP-path results.\n- `COVERAGE_STALE` bug fixed (tests are `atomic_process` with `subtype=test`, query now matches).\n- Verification tracks both `code_hash` and `desc_hash` — future drift in either is detected after verification. Fixes silent loss of `DESC_STALE` after verifying a `CONTENT_STALE` node.\n- \"Both changed\" bug fixed — when code and desc both change, `CONTENT_STALE` is emitted instead of incorrectly falling through to `CLEAN`.\n- Doc sections become verifiable — previously blocked by `node_type == \"document\"` guard.\n- Doc files and sections gain hash-similarity rename detection for free — moving a doc file or renaming a section ID preserves history and verification.\n- Composite inheritance severity map includes all statuses — `CONTENT_STALE` and `LINKED_STALE` propagate correctly instead of silently mapping to `CLEAN`.\n\n**Negative:**\n- Re-init required: all existing DBs must run `cortex init` to get new schema (`staleness` column on `nodes`, `desc_hash_at` on `node_verification`). History and verifications are reset.\n- All code branching on `node_type == 'document'` or `'constraint'` must use `subtype` instead.\n- Viz color scheme must switch from `node_type` to `subtype` keying.\n- Section `code_hash`/`desc_hash` semantics are swapped from the current inverted layout — any tooling that assumed the old mapping must be updated.\n\n**Neutral:**\n- `entity` nodes unchanged.\n- `node_history.change_type` values (`CODE_ONLY`, `DESC_ONLY`) unchanged — only the emitted status vocabulary changes.\n- `tags` table unchanged.\n- `_compute_staleness_rich` remains the authoritative computation, just becomes the only writer and is extracted to a shared module.\n- `supersedes` edge type retained in ontology (valid for ADR relationships) with updated `from:`/`to:` rules."
    }
  ]
}