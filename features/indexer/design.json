{
  "title": "Design Spec: Indexer",
  "tags": ["design", "v1", "indexer"],
  "sections": [
    {
      "id": "architecture",
      "heading": "Architecture Overview",
      "content": "The indexer is the core engine of cortex. It scans a project directory, builds a knowledge graph of nodes (functions, modules, documents) and edges (composes, delegates_to, validates, documents), and stores everything in `.cortex/cortex.db` (SQLite).\n\n```\ncortex/\n  cli.py                # Click CLI — thin wrappers over index/*\n  models.py             # Pure dataclasses: CortexNode, CortexEdge, CortexIndex, StepMarker\n  ontology.py           # Loads cortex_ontology.yaml, validates edge types\n  index/\n    builder.py          # Orchestrates scanning → upsert pipeline\n    db.py               # SQLite schema, upsert_node, query helpers, FTS5\n    runner.py           # Pytest runner detection, report format selection\n    junit_parser.py     # Parses JUnit XML and pytest-json-report output\n    test_mapper.py      # Maps pytest IDs → cortex node IDs via validates edges\n  scanners/\n    module_scanner.py   # AST-based Python scanner: functions, classes, modules\n    doc_scanner.py      # Markdown doc scanner\n    json_doc_scanner.py # DocJSON scanner: sections, links → edges\n```\n\nThe pipeline is: `cortex init` (full baseline) or `cortex build` (discovery-only) → scanners produce `CortexNode`/`CortexEdge` lists → `builder.py` orchestrates upserts → `db.py` writes to SQLite with incremental change detection.\n\nAll read paths (CLI commands, viz server, MCP tools) go through `db.py` query functions. No business logic lives outside the index package."
    },
    {
      "id": "sequence-diagram",
      "heading": "Sequence of Operations",
      "content": "### cortex build (discovery-only, default)\n```\ncli.cmd_build(project_root)\n  → builder.build(root, discovery_only=True)\n    → module_scanner scans all .py files via AST\n      → Per file: parse → walk AST → extract functions/classes\n      → Compute code_hash (SHA-256[:16] of body) and desc_hash (of docstring)\n      → Generate CortexNode per function + composite_process per module\n      → Generate composes edges (module → function)\n      → Detect dFlow decorators → store in dflow_meta\n    → json_doc_scanner scans docs/**/*.json\n      → Parse sections, extract links → documents edges\n    → For each node: db.upsert_node()\n      → Compare code_hash + desc_hash against stored values\n      → If unchanged: skip (returns False)\n      → If changed: write + append node_history row (CODE_ONLY/DESC_ONLY/CODE_AND_DESC)\n      → If new: write + append INITIAL history row\n    → For each edge: db.upsert_edge()\n    → Return summary dict {nodes_written, nodes_skipped, edges_written, ...}\n```\n\n### cortex check\n```\nFor each file in the project:\n  1. stat() → compare mtime against stored file_mtime\n  2. If unchanged → all nodes in file are CLEAN (fast path)\n  3. If changed → re-parse with ast.parse()\n     → Per function: _split_function() → separate code body from docstring\n     → Hash both → compare against stored code_hash / desc_hash\n     → CODE changed but DESC unchanged → DESC_STALE\n     → Function missing from AST → STRUCTURAL_DRIFT\n     → No stored hash → UNVERIFIED\n  4. Verification promotion: check node_verification table\n     → If code_hash_at matches current code_hash → VERIFIED\n     → CONTENT_STALE (doc nodes) never promoted\n```",
      "level": 2
    },
    {
      "id": "data-modeling",
      "heading": "Data Model & Graph Schema",
      "content": "### Core Tables\n\n| Table | Purpose |\n|---|---|\n| `nodes` | All indexed entities: functions, modules, docs, external packages |\n| `edges` | Typed relationships: composes, delegates_to, validates, documents, etc. |\n| `tags` | Many-to-many node↔tag |\n| `node_fts` | FTS5 virtual table over level_1/level_2 for full-text search |\n| `node_history` | Append-only change log per node (INITIAL, CODE_ONLY, DESC_ONLY, etc.) |\n| `node_verification` | Verification records: who verified, at what code_hash |\n| `node_renames` | Tracks old_id → new_id when functions are renamed |\n| `docs` | DocJSON file-level records |\n| `doc_sections` | Individual sections within DocJSON files |\n| `test_runs` | Pytest run metadata (timestamp, git SHA, duration, summary) |\n| `test_results` | Per-test results linked to test_runs and optionally to cortex nodes |\n\n### Node Types (from ontology)\n\n| Node Type | Subtypes | What it represents |\n|---|---|---|\n| `atomic_process` | — | A single function or method |\n| `composite_process` | — | A module, package, workflow, or pipeline |\n| `entity` | data_artifact, config_artifact, model_artifact, external_package | Data consumed/produced |\n| `constraint` | test, decision, schema, contract | Tests, schemas, decisions |\n| `document` | — | Markdown/DocJSON documentation |\n\n### Edge Types (from ontology)\n\n| Edge Type | From → To | Meaning |\n|---|---|---|\n| `consumes` | process → entity | Input data flow |\n| `produces` | process → entity | Output data flow |\n| `composes` | composite → process | Structural containment |\n| `delegates_to` | composite → process | Runtime invocation |\n| `depends_on` | process → process/entity | Execution dependency |\n| `constrains` | constraint → process/entity | Schema/contract restriction |\n| `validates` | process/constraint → process | Test coverage |\n| `documents` | document → any | Documentation link |\n| `supersedes` | process/constraint → same | Replacement/obsolescence |",
      "level": 2
    },
    {
      "id": "decision-log",
      "heading": "Implementation Trade-offs",
      "content": "### AST-based scanning over regex\nPython's `ast` module correctly handles nested functions, decorators, multi-line docstrings, and async defs. Regex would fail on all of these. The cost is Python-only support for per-function granularity; non-Python files get whole-file hashing.\n\n### Incremental upserts with change detection\n`upsert_node` compares `code_hash + desc_hash` before writing. If both match, the row is skipped entirely — no write, no history row. This keeps `cortex build` fast on unchanged projects (< 1s on ~300 nodes) and prevents history pollution.\n\n### Discovery-only as the default build mode\nThe default `cortex build` only inserts new nodes and never updates existing ones. This preserves DESC_STALE signals that haven't been resolved yet. To reset all baselines, use `cortex init` — there is no `--full` flag on `cortex build`.\n\n### cortex init as the safe first-run and reset command\n`cortex init` creates the database, runs a full scan, and establishes baselines for every node. If the database already exists, it prompts for confirmation before wiping and rebuilding. This separates the destructive reset path from the safe everyday `cortex build` path.\n\n### FTS5 with three-stage fallback\nSearch uses: (1) FTS5 ranked search, (2) LIKE-AND substring fallback, (3) LIKE-OR broad fallback. This ensures something always comes back, even for malformed queries.",
      "level": 2,
      "tags": ["decisions"]
    },
    {
      "id": "verification-plan",
      "heading": "Technical Verification",
      "content": "### Existing Tests\nUnit and integration tests exist in the cortex test suite covering:\n- Module scanner: function extraction, hash computation, dFlow decorator detection\n- DB layer: upsert_node change detection, history row generation, FTS search\n- Builder: end-to-end build on fixture projects\n- Staleness: mtime fast path, hash-based detection, verification promotion\n- Test runner: JUnit parsing, test-mapper edge resolution\n\n### Edge Cases to Cover\n- Nested functions (inner function hashes must not leak into outer)\n- Renamed functions (STRUCTURAL_DRIFT detection + node_renames tracking)\n- Empty docstrings vs missing docstrings (different desc_hash values)\n- Unicode in function names and docstrings\n- Files with syntax errors (should degrade to STRUCTURAL_DRIFT, not crash)\n- Concurrent builds (SQLite single-writer — should fail gracefully)",
      "level": 2
    }
  ]
}