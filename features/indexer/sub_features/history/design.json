{
  "title": "Design Spec: History",
  "tags": [
    "design",
    "v1",
    "indexer",
    "history"
  ],
  "sections": [
    {
      "id": "architecture",
      "heading": "Architecture Overview",
      "content": "The history subsystem provides an append-only audit trail for every node in the index. Every time `db.upsert_node()` detects a change, it inserts a `node_history` row recording what changed and when.\n\nHistory data lives in two tables:\n- `node_history` — append-only log with change_type, timestamp, optional git SHA, and metadata\n- `node_verification` — current verification state per node (see ADR-003)\n\n### Change type families\n\nChange types fall into three families based on what triggers them:\n\n**Content events** — recorded by the scanner when it detects hash differences during `upsert_node`. These answer \"what physically changed in the source.\" The names are content-centric (not code-centric) because they apply uniformly to both function nodes and doc section nodes.\n\n| Type | Trigger |\n|---|---|\n| `INITIAL` | Node first discovered |\n| `CONTENT_ONLY` | code_hash changed, desc_hash unchanged (renamed from `CODE_ONLY` — applies to function bodies and doc section prose alike) |\n| `DESC_ONLY` | desc_hash changed, code_hash unchanged |\n| `CONTENT_AND_DESC` | Both changed (renamed from `CODE_AND_DESC`) |\n\nFor function nodes, `CONTENT_ONLY` means the function body changed. For doc sections (`subtype=docjson`), `CONTENT_ONLY` means the prose body changed. The scanner already fires the correct event for both via `upsert_node_conn` — no separate doc-side event types are needed.\n\n**State events** — recorded by `record_staleness` when the node's staleness status transitions. These answer \"how did the staleness engine interpret the change.\" (Backlog — not yet implemented.)\n\n| Type | Trigger |\n|---|---|\n| `BECAME_CONTENT_STALE` | CLEAN/VERIFIED → CONTENT_STALE |\n| `BECAME_DESC_STALE` | CLEAN/VERIFIED → DESC_STALE |\n| `BECAME_LINKED_STALE` | CLEAN → LINKED_STALE |\n| `BECAME_STRUCTURAL_DRIFT` | any → STRUCTURAL_DRIFT |\n| `RESOLVED` | any stale state → CLEAN (hashes re-aligned) |\n\nState events use `preserved=0` — they are subject to the same 100-row hard cap and checkpoint pruning as content events. This avoids unbounded accumulation for high-churn nodes while still capturing recent staleness lifecycles for impact reports and stale window queries. Old transitions are collapsed by `cortex history checkpoint` alongside content events.\n\n**Actor events** — recorded when a human, agent, or system takes an explicit action. These answer \"who did what.\"\n\n| Type | Trigger |\n|---|---|\n| `AGENT_VERIFIED` | AI agent marked node as verified |\n| `MANUAL_VERIFIED` | Human marked node as verified |\n| `CHECKPOINT` | History collapsed at this point |\n\nAll three families share the same `node_history` table and `change_type` column. The family distinction is conceptual — it helps the history tab, `cortex report`, and MCP tools group and filter events meaningfully.\n\n### Dropped: DOC_UPDATED, DOC_CREATED, LINK_ADDED\n\nThe original design proposed separate actor-event types for doc edits via MCP tools. These are no longer needed. When `cortex_update_section` writes to disk and triggers a re-index, `upsert_node_conn` detects the hash change and records a `CONTENT_ONLY` or `DESC_ONLY` row on the doc section node. Impact reports distinguish code vs doc changes by checking the node's `subtype` (function vs docjson), not the change type.\n\n`LINK_ADDED` (adding a `documents` edge) does not change prose body or heading hashes, so it produces no history row. This is acceptable — link additions are structural metadata, not content changes. The edges table can be queried directly for edge-level audit if needed in the future.\n\n### record_staleness: the unified write point for staleness + transition events\n\n`compute_staleness` remains a pure computation: it takes nodes, returns `dict[str, str]`, and does not write to the history table. A new wrapper — `record_staleness(db_path, project_root, nodes)` — becomes the single write point that all callers (CLI, MCP, viz) use:\n\n```\nrecord_staleness(db_path, project_root, nodes):\n    with _connect(db_path) as conn:\n        old_statuses = {row.id: row.staleness for row in read nodes table}\n        new_statuses = compute_staleness(db_path, project_root, nodes)\n        for node_id in new_statuses:\n            old = old_statuses.get(node_id, \"CLEAN\")\n            new = new_statuses[node_id]\n            if old != new:\n                insert transition event (BECAME_CONTENT_STALE / RESOLVED / etc.)\n        UPDATE nodes SET staleness = new for all nodes\n```\n\nThis keeps `compute_staleness` testable with just a dict return, while ensuring transition events and the staleness column are always written in the same transaction — a crash between steps cannot leave them inconsistent.\n\nThe three current callers (`cmd_check`, `cortex_build`, viz `/api/check`) replace their individual write-back code with a single `record_staleness` call.\n\n### git_sha population\n\nEvery content-event row should carry the HEAD SHA at build time. `build()` calls `get_git_sha()` once at start and threads it through to `upsert_node_conn`. Actor events (`AGENT_VERIFIED`, `MANUAL_VERIFIED`, `CHECKPOINT`) already populate `git_sha`. State events will populate it when implemented via `record_staleness`.\n\nThe SHA enables diff retrieval via `git show {sha}:{file_path}` — no source snapshots are stored in the database. If the SHA becomes unreachable (rebase, GC), diffs gracefully degrade; the history row itself remains valid.\n\nRows with `preserved=1` (verifications, checkpoints) are exempt from pruning.",
      "level": 2
    },
    {
      "id": "sequence-diagram",
      "heading": "Sequence of Operations",
      "content": "### History recording (automatic, during cortex build)\n```\ndb.upsert_node(path, node):\n  existing = SELECT code_hash, desc_hash FROM nodes WHERE id = node.id\n  IF not existing → INSERT + history(INITIAL)\n  IF code_hash changed AND desc_hash changed → UPDATE + history(CODE_AND_DESC)\n  IF code_hash changed only → UPDATE + history(CODE_ONLY)\n  IF desc_hash changed only → UPDATE + history(DESC_ONLY)\n  IF neither changed → skip (no write, no history)\n```\n\n### Checkpoint & pruning\n```\ncortex history checkpoint .\n  For each qualifying node (by --node-types):\n    1. Find old collapsible rows (CODE_ONLY, DESC_ONLY, CODE_AND_DESC) older than --older-than days\n    2. Insert CHECKPOINT row timestamped at oldest collapsible row (preserved=1)\n    3. Keep --keep most-recent ordinary rows, delete the rest\n    AGENT_VERIFIED and MANUAL_VERIFIED rows are never deleted (preserved=1)\n```\n\nConfiguration options for `cortex history checkpoint`:\n\n| Option | Description | Default |\n|---|---|---|\n| `--keep N` | Rows to retain per node | 10 |\n| `--older-than N` | Only prune entries older than N days | 90 |\n| `--node-types TYPES` | Comma-separated types to apply to | `atomic_process,composite_process` |\n| `--no-prune` | Insert checkpoint without deleting rows | Off |\n\n### Pre-push gate workflow\n```\ncortex history agent-verified .\n  Query node_verification WHERE verified_by LIKE 'agent%'\n    AND no subsequent MANUAL_VERIFIED row exists\n  Returns list of nodes where AI approved docs but human hasn't reviewed\n  Intended as a pre-push or pre-merge check\n```\n\n### Stale window annotation (cortex_history MCP tool)\n```\nWalk history newest-first:\n  Track whether a DESC_ONLY or CODE_AND_DESC has been seen\n  If CODE_ONLY and no desc update above it → \"stale window opened (N days)\"\n  Annotates each row to show how long the docs have been drifting\n```",
      "level": 2
    },
    {
      "id": "decision-log",
      "heading": "Implementation Trade-offs",
      "content": "### Append-only with pruning, not rolling window\nWe keep full history by default and only prune via explicit `checkpoint`. This means history survives across multiple build cycles. The `--keep N` flag prevents unbounded growth for high-churn functions.\n\n### preserved=1 for verification rows\nVerification history is never pruned because it's the audit trail for the pre-push gate. Without it, `cortex history agent-verified` couldn't list pending reviews.\n\n### _HISTORY_ROW_LIMIT = 100\nA hard cap in db.py prevents any single node from accumulating more than 100 non-preserved rows, even without running checkpoint.\n\n### CONTENT_ONLY replaces CODE_ONLY (and drops DOC_UPDATED)\nThe original design had `CODE_ONLY` for content events and proposed separate `DOC_UPDATED`/`DOC_CREATED`/`LINK_ADDED` actor events for doc edits via MCP tools. This created a deduplication problem: a `cortex_update_section` call would produce both a rich `DOC_UPDATED` event (from the MCP tool) and a generic `CODE_ONLY` event (from the subsequent re-index via `upsert_node_conn`).\n\n**Decision:** Rename `CODE_ONLY` → `CONTENT_ONLY` and `CODE_AND_DESC` → `CONTENT_AND_DESC`. Drop `DOC_UPDATED`, `DOC_CREATED`, and `LINK_ADDED` entirely. The scanner already fires the correct content-change event for both function nodes and doc section nodes — the names were just code-centric. Impact reports distinguish code vs doc changes by checking the node's `subtype` (function vs docjson), not the change type. This eliminates the double-write problem and keeps the change type vocabulary small.\n\n### record_staleness wrapper over compute_staleness\n`compute_staleness` returns `dict[str, str]` and does not write history. A new `record_staleness` wrapper reads old staleness values, calls `compute_staleness`, diffs old vs new, writes transition events (`BECAME_CONTENT_STALE`, `RESOLVED`, etc.), and updates the `staleness` column — all in one transaction.\n\n**Why not inline in compute_staleness?** `compute_staleness` is already 200+ lines with five steps. Adding transition logic would make it harder to test and reason about. The wrapper keeps the computation pure and testable with just a dict return, while the wrapper owns the DB write side effects. The three callers (CLI, MCP, viz) replace their individual write-back code with one `record_staleness` call.\n\n**Transaction scope:** The wrapper reads old statuses, computes new statuses, writes transition events, and updates the staleness column in a single `_connect(db_path)` context manager. A crash between steps cannot leave transition events and the staleness column inconsistent.\n\n### State events use preserved=0\nStaleness transition events (`BECAME_CONTENT_STALE`, `RESOLVED`, etc.) use `preserved=0`, making them subject to the same 100-row hard cap and `cortex history checkpoint` pruning as content events. This avoids unbounded accumulation for high-churn nodes. Only verification rows (`AGENT_VERIFIED`, `MANUAL_VERIFIED`) and checkpoints use `preserved=1`. Old transitions are collapsed alongside content events during checkpoint — the checkpoint row serves as the summary boundary.",
      "level": 2,
      "tags": [
        "decisions"
      ]
    },
    {
      "id": "verification-plan",
      "heading": "Technical Verification",
      "content": "- Change code only → CODE_ONLY row inserted\n- Change docstring only → DESC_ONLY row inserted\n- Change both → CODE_AND_DESC row inserted\n- No change → no history row (skip)\n- Checkpoint with --no-prune → CHECKPOINT row added, no rows deleted\n- Checkpoint with --keep 5 → oldest rows beyond 5 deleted, verifications preserved\n- Stale window annotation correctly identifies the first CODE_ONLY after the last desc update",
      "level": 2
    }
  ]
}