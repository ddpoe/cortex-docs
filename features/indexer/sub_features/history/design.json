{
  "title": "Design Spec: History",
  "tags": [
    "design",
    "v1",
    "indexer",
    "history"
  ],
  "sections": [
    {
      "id": "architecture",
      "heading": "Architecture Overview",
      "content": "The history subsystem provides an append-only audit trail for every node in the index. Every time `db.upsert_node()` detects a change, it inserts a `node_history` row recording what changed and when.\n\nHistory data lives in two tables:\n- `node_history` — append-only log with change_type, timestamp, optional git SHA, and metadata\n- `node_verification` — current verification state per node (see ADR-003)\n\n### Change type families\n\nChange types fall into three families based on what triggers them:\n\n**Content events** — recorded by the scanner when it detects hash differences during `upsert_node`. These answer \"what physically changed in the source.\"\n\n| Type | Trigger |\n|---|---|\n| `INITIAL` | Node first discovered |\n| `CODE_ONLY` | code_hash changed, desc_hash unchanged |\n| `DESC_ONLY` | desc_hash changed, code_hash unchanged |\n| `CODE_AND_DESC` | Both changed |\n\n**State events** — recorded by `compute_staleness` when the node's staleness status transitions. These answer \"how did the staleness engine interpret the change.\" (Backlog — not yet implemented.)\n\n| Type | Trigger |\n|---|---|\n| `BECAME_CONTENT_STALE` | CLEAN/VERIFIED → CONTENT_STALE |\n| `BECAME_DESC_STALE` | CLEAN/VERIFIED → DESC_STALE |\n| `BECAME_LINKED_STALE` | CLEAN → LINKED_STALE |\n| `BECAME_STRUCTURAL_DRIFT` | any → STRUCTURAL_DRIFT |\n| `RESOLVED` | any stale state → CLEAN (hashes re-aligned) |\n\n**Actor events** — recorded when a human, agent, or system takes an explicit action. These answer \"who did what.\"\n\n| Type | Trigger |\n|---|---|\n| `AGENT_VERIFIED` | AI agent marked node as verified |\n| `MANUAL_VERIFIED` | Human marked node as verified |\n| `CHECKPOINT` | History collapsed at this point |\n| `DOC_UPDATED` | `cortex_update_section` changed doc content (backlog) |\n| `DOC_CREATED` | `cortex_write_doc` created a new doc (backlog) |\n| `LINK_ADDED` | `cortex_add_link` established a doc-to-code link (backlog) |\n\nAll three families share the same `node_history` table and `change_type` column. The family distinction is conceptual — it helps the history tab, `cortex report`, and MCP tools group and filter events meaningfully.\n\n### git_sha population\n\nEvery content-event row should carry the HEAD SHA at build time. `build()` calls `get_git_sha()` once at start and threads it through to `upsert_node_conn`. Actor events (`AGENT_VERIFIED`, `MANUAL_VERIFIED`, `CHECKPOINT`) already populate `git_sha`. State events will populate it when implemented.\n\nThe SHA enables diff retrieval via `git show {sha}:{file_path}` — no source snapshots are stored in the database. If the SHA becomes unreachable (rebase, GC), diffs gracefully degrade; the history row itself remains valid.\n\nRows with `preserved=1` (verifications, checkpoints) are exempt from pruning.",
      "level": 2
    },
    {
      "id": "sequence-diagram",
      "heading": "Sequence of Operations",
      "content": "### History recording (automatic, during cortex build)\n```\ndb.upsert_node(path, node):\n  existing = SELECT code_hash, desc_hash FROM nodes WHERE id = node.id\n  IF not existing → INSERT + history(INITIAL)\n  IF code_hash changed AND desc_hash changed → UPDATE + history(CODE_AND_DESC)\n  IF code_hash changed only → UPDATE + history(CODE_ONLY)\n  IF desc_hash changed only → UPDATE + history(DESC_ONLY)\n  IF neither changed → skip (no write, no history)\n```\n\n### Checkpoint & pruning\n```\ncortex history checkpoint .\n  For each qualifying node (by --node-types):\n    1. Find old collapsible rows (CODE_ONLY, DESC_ONLY, CODE_AND_DESC) older than --older-than days\n    2. Insert CHECKPOINT row timestamped at oldest collapsible row (preserved=1)\n    3. Keep --keep most-recent ordinary rows, delete the rest\n    AGENT_VERIFIED and MANUAL_VERIFIED rows are never deleted (preserved=1)\n```\n\nConfiguration options for `cortex history checkpoint`:\n\n| Option | Description | Default |\n|---|---|---|\n| `--keep N` | Rows to retain per node | 10 |\n| `--older-than N` | Only prune entries older than N days | 90 |\n| `--node-types TYPES` | Comma-separated types to apply to | `atomic_process,composite_process` |\n| `--no-prune` | Insert checkpoint without deleting rows | Off |\n\n### Pre-push gate workflow\n```\ncortex history agent-verified .\n  Query node_verification WHERE verified_by LIKE 'agent%'\n    AND no subsequent MANUAL_VERIFIED row exists\n  Returns list of nodes where AI approved docs but human hasn't reviewed\n  Intended as a pre-push or pre-merge check\n```\n\n### Stale window annotation (cortex_history MCP tool)\n```\nWalk history newest-first:\n  Track whether a DESC_ONLY or CODE_AND_DESC has been seen\n  If CODE_ONLY and no desc update above it → \"stale window opened (N days)\"\n  Annotates each row to show how long the docs have been drifting\n```",
      "level": 2
    },
    {
      "id": "decision-log",
      "heading": "Implementation Trade-offs",
      "content": "### Append-only with pruning, not rolling window\nWe keep full history by default and only prune via explicit `checkpoint`. This means history survives across multiple build cycles. The `--keep N` flag prevents unbounded growth for high-churn functions.\n\n### preserved=1 for verification rows\nVerification history is never pruned because it's the audit trail for the pre-push gate. Without it, `cortex history agent-verified` couldn't list pending reviews.\n\n### _HISTORY_ROW_LIMIT = 100\nA hard cap in db.py prevents any single node from accumulating more than 100 non-preserved rows, even without running checkpoint.",
      "level": 2,
      "tags": [
        "decisions"
      ]
    },
    {
      "id": "verification-plan",
      "heading": "Technical Verification",
      "content": "- Change code only → CODE_ONLY row inserted\n- Change docstring only → DESC_ONLY row inserted\n- Change both → CODE_AND_DESC row inserted\n- No change → no history row (skip)\n- Checkpoint with --no-prune → CHECKPOINT row added, no rows deleted\n- Checkpoint with --keep 5 → oldest rows beyond 5 deleted, verifications preserved\n- Stale window annotation correctly identifies the first CODE_ONLY after the last desc update",
      "level": 2
    }
  ]
}