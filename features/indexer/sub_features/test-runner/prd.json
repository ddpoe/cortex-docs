{
  "title": "PRD: Test Runner",
  "tags": [
    "prd",
    "v1",
    "indexer",
    "test-runner",
    "deprecated"
  ],
  "sections": [
    {
      "id": "problem",
      "heading": "Problem Statement",
      "content": "> **DEPRECATED:** The test runner and test dashboard execution/results features are deprecated. The viz test tab is being simplified to test discovery and annotation visibility only — showing test functions, their tier classification, purpose strings, step narratives, and validates edges. Pass/fail badges, JUnit ingestion, the Run Tests button, and test run history are being removed. Test execution belongs in the terminal.\n\nThe test runner can already scope test runs to specific functions and modules via `validates` edges, but it can only do so when those edges exist. Currently `validates` edges come from two sources, both flawed: dFlow's `covers=` parameter requires manual annotation on a decorator (coupling test infrastructure to dFlow, drifting as tests evolve, and excluding projects that don't use dFlow), and manual `cortex link` commands (too tedious to maintain at scale). The test runner needs automatic, annotation-free `validates` edges generated by static AST analysis at scan time, with dFlow having no role in test→code linking."
    },
    {
      "id": "user-stories",
      "heading": "User Stories",
      "content": "**US-TR-01 — Run tests for a specific function**\nAs a developer, I want to run all tests that cover a specific function by naming that function so that I can validate a targeted change without running the full suite.\n\n**US-TR-02 — Run tests for a module**\nAs a developer, I want to run all tests that cover any function in a module by naming that module so that I can validate a module-level refactor in one command.\n\n**US-TR-03 — Store results for impact report**\nAs an agent, I want to run all tests associated with a function or module and have the results stored in the index so that test outcomes appear as verification evidence in the impact report.\n\n**US-TR-04 — Automatic test-to-code linking**\nAs a developer, I want test→code relationships to be detected automatically from the test source code so that I never need to maintain manual annotations or dFlow `covers=` parameters.\n\n**US-TR-05 — No dFlow dependency for test linking**\nAs a developer, I want test coverage mapping to work on any Python project so that projects not using dFlow get the same scoped test runner behavior."
    },
    {
      "id": "current-capabilities",
      "heading": "Current Capabilities",
      "content": "| Capability | Status |\n|---|---|\n| `cortex test` command | **Removed** (A5 deprecation) |\n| Runner detection (pixi / poetry / plain pytest) | **Removed** (A5 deprecation) |\n| JUnit XML report parsing | **Removed** (A5 deprecation) |\n| pytest-json-report parsing (preferred) | **Removed** (A5 deprecation) |\n| `--node <node_id>` scoping via `validates` edges | **Removed** (A5 deprecation) |\n| `--path <prefix>` scoping via module prefix | **Removed** (A5 deprecation) |\n| `--no-save` flag | **Removed** (A5 deprecation) |\n| `test_runs` and `test_results` tables | **Removed** from schema (A5 deprecation); old tables left in existing DBs |\n| Unmapped test flagging in summary output | **Removed** (A5 deprecation) |\n| `validates` edges from dFlow `covers=` parameter | Done (retained — discovery only) |\n| AST call graph `validates` edges (no dFlow dependency) | Done (retained — discovery only) |\n| Migration path off dFlow `covers=` | Not started |\n\nAll test execution machinery (runner, parser, mapper, CLI command, DB tables, viz run UI) was removed in PEV cycle `pev-2026-03-27-deprecate-test-runner`. Test node scanning, tier annotations, step narratives, and `validates` edges are preserved."
    },
    {
      "id": "call-graph-design",
      "heading": "AST Call Graph Design",
      "content": "The call graph scanner replaces dFlow `covers=` as the source of `validates` edges. It runs as part of the module scanner pipeline during `cortex build`.\n\n**How it works:**\n\n1. Detect test files by convention (`test_*.py`, `*_test.py`) or by the presence of pytest markers.\n2. For each test function in the file, build an import name→module map from the file's `import` and `from … import` statements.\n3. Walk the function body AST, collecting all `Call` nodes (direct function calls only — no transitive traversal).\n4. For each call, resolve the called name against the import map to get a qualified module path (e.g. `from pm.method import execute` + call `execute(...)` → `pm.method.execute`).\n5. Look up the resolved qualified name as a cortex node ID. If found → emit a `validates` edge from the test node to the production node. If not found (stdlib, third-party, unresolvable) → silently skip.\n\n**What this handles correctly:**\n\n| Pattern | Result |\n|---|---|\n| `from pm.method import execute; execute(...)` | Edge created |\n| `import pm.method; pm.method.execute(...)` | Edge created (attribute access on import alias) |\n| `numpy.max(...)` | Silently ignored — not in cortex index |\n| Parameterized test calling multiple functions | All resolvable calls get edges |\n| Fixture-mediated call (`client.execute()`) | No edge — type unknown without inference |\n| Dynamic dispatch (`getattr(obj, fn)()`) | No edge — unresolvable statically |\n\n**Scope:** direct calls only. Transitive coverage (test → A → B → C) is what runtime tools like `pytest-cov` provide. Static call graph edges represent what the test *directly* exercises, which is the right scope for focused test runs.\n\n**dFlow migration:** once call graph edges are live, `covers=` on `@test_workflow` becomes redundant. Running `cortex build` after the feature ships auto-generates equivalent edges. dFlow can remove `covers=` from its decorator API in a subsequent release."
    },
    {
      "id": "backlog",
      "heading": "Backlog",
      "content": "~~**AST call graph `validates` edges**~~ -- Completed. Implemented in module_scanner via enriched `name_map` and `_collect_functions`. See the Call Graph Design section above for the original specification.\n\n**Fixture-mediated call resolution** — Tests that exercise production code through a pytest fixture (e.g. `def test_x(client): client.execute()`) cannot be resolved by static AST without type inference. V1 accepts this gap: such tests are unmapped and flagged in the summary. Manual `cortex link` is the escape hatch.\n\n**`covers=` migration tooling** — A one-time `cortex migrate-covers` command that reads existing dFlow `covers=` annotations and emits the equivalent `cortex link` commands, allowing projects to audit the mapping before deleting the dFlow annotations.\n\n**Transitive coverage queries** — A read-only query (not edge generation): given a node, which tests transitively cover it by following the call graph? Useful for understanding the full test surface before a deep refactor. Distinct from the direct-call edges used for test scoping."
    }
  ]
}